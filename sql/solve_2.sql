/*

Query all columns (attributes) for every row in the CITY table.

The CITY table is described as follows:

field                         type
id                            number
name                          varchar
countrycode                   varchar
district                      varchar
population                    number
*/

/* query */
select * from CITY;